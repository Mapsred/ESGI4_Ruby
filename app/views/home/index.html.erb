<h1>
  Ceci est la page d'accueil du projet
  <%= Rails.application.class.parent_name %> !
</h1>

<% if @expedition.any? %>
  <p>Une expédition est en cours jusqu'au <%= @expedition.end_date.to_formatted_s('%B %d %Y') %></p>
<% else %>
  <p>Aucune expédition en cours, vous pouvez en démarrer une ici : </p>
  <a href="<%= new_expedition_path %>" type="button" class="btn btn-default">
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Ajouter une expédition
  </a>
<% end %>