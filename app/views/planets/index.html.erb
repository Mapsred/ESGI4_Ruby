<div class="row">
  <div class="col-md-12">
    <h1>Planètes</h1>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <% if @planets.any? %>
      <table class="table" id="dataTable" style="width: 100%;">
        <thead>
        <tr>
          <th>ID</th>
          <th>Nom</th>
          <th>
            <div class="col-md-2 col-md-offset-10">Action</div>
          </th>
        </tr>
        </thead>
        <tbody>
        <% @planets.each do |planet| %>
          <tr>
            <td><%= planet.id %></td>
            <td><%= planet.name %></td>
            <td>
              <div class="btn-group pull-right">
                <a href="<%= planet_path planet %>" class="btn btn-success">
                  <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Détails
                </a>
                <% if user_signed_in? %>
                <a href="<%= edit_planet_path planet %>" class="btn btn-info">
                  <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Éditer
                </a>
                <%= link_to raw('<span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Supprimer'),
                            planet,
                            method: :delete,
                            data: {
                                confirm: 'Êtes-vous sûr(e) de vouloir supprimer cette planète ?'
                            },
                            class: 'btn btn-danger'
                %>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
        </tbody>
        <tfoot>
        <tr>
          <th>ID</th>
          <th>Nom</th>
          <th class="disabled">Action</th>
        </tr>
        </tfoot>
      </table>
    <% else %>
      <div class="alert alert-info" role="alert">
        Aucune planète
      </div>
    <% end %>
  </div>
</div>

<% if user_signed_in? %>
<div class="row">
  <div class="col-md-6 col-md-offset-6">
    <a href="<%= new_planet_path %>" type="button" class="btn btn-default pull-right">
      <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Ajouter une planète
    </a>
  </div>
</div>
<% end %>