<div class="nav-header">
  <a class="btn btn-primary btn-sm" href="<%= astronauts_path %>"><span class="glyphicon glyphicon-chevron-left"></span>
    Back</a>
  <h3>
    <% if @astronaut.grade.nil? %>
      <%= @astronaut.name %>
    <% else %>
      <%= image_tag("icon/icons8-caporal-50.png", :alt => "grade major") %>
      <%= grade_user(@astronaut.name, @astronaut.grade.name) %>
    <% end %>
  </h3>
</div>

<div class="well">
  <div class="row">

    <div class="col-md-12">
      <label class="col-sm-2">Name</label>
      <span><%= @astronaut.name.nil? ? '&empty;' : @astronaut.name %></span>
    </div>

    <div class="col-md-12">
      <label class="col-sm-2">Mail</label>
      <span><%= @astronaut.mail.nil? ? '&empty;' : @astronaut.mail %></span>
    </div>

    <div class="col-md-12">
      <label class="col-sm-2">Planet</label>
      <span><%= @astronaut.planet.nil? ? '&empty;' : @astronaut.planet.name %></span>
    </div>

  </div>
</div>